<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Asked for at the moment it explains itself, not on first launch. See NudgePolicy. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Dictating a note. On-device recognition only — see VoiceNote for why the network kind is
         refused rather than used as a fallback. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Required on Android 11+ for SpeechRecognizer to see an on-device recogniser at all. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".TwofoldApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Twofold">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Twofold"
            android:resizeableActivity="true"
            android:screenOrientation="fullUser"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
